---
# Red Hat Insights has recommended one or more actions for you, a system administrator, to review and if you
# deem appropriate, deploy on your systems running Red Hat software. Based on the analysis, we have automatically
# generated an Ansible Playbook for you. Please review and test the recommended actions and the Playbook as
# they may contain configuration changes, updates, reboots and/or other changes to your systems. Red Hat is not
# responsible for any adverse outcomes related to these recommendations or Playbooks.
#
# Addresses maintenance plan 36428 (dnsmasq)
# https://access.redhat.com/insights/planner/36428
# Generated by Red Hat Insights on Fri, 31 Aug 2018 20:33:34 GMT

- name: run insights to obtain latest report info
  hosts: "{{HOSTS}}"
  become: True
  tasks:
    - name: determine insights version
      shell: 'redhat-access-insights --version'
      changed_when: false
      register: insights_version

    - when: insights_version.stdout[0:2] != '1.'
      block:
        - name: obtaining insights report
          shell: 'redhat-access-insights --to-json --quiet'
          register: insights_result
          changed_when: false
          check_mode: false
        - name: register insights report as fact for use by other plays
          set_fact: insights_report={{ insights_result.stdout }}

    - when: insights_version.stdout[0:2] == '1.'
      block:
        - name: obtaining insights report (legacy client)
          shell: 'redhat-access-insights --verbose | grep "Upload status: 201 Created" | grep -o "{.*}"'
          register: insights_result_legacy
          changed_when: false
          check_mode: false
        - name: register insights report as fact for use by other plays (legacy client)
          set_fact: insights_report={{ insights_result_legacy.stdout }}

- name: Modify AMQ Journal Pool Files 
  hosts: "{{HOSTS}}"
  become: true
  vars:
    pydata: "{{insights_report.details['journal_pool_files|JOURNAL_POOL_FILES_OLD_DEFAULT'] | default('')}}"
  tasks:
    - when:
        - pydata.files is defined
      block:
      - name: Replace the journal-pool-files value
        lineinfile:
          backup: yes
          backrefs: yes
          path: "{{item}}"
          regexp: '(^\s*\<journal-pool-files\>)(-1)(\<\/journal-pool-files\>)'
          line: '\1 10\3'
        with_items:
          - "{{pydata.files}}"
		
- name: run insights
  hosts: "{{HOSTS}}"
  become: True
  gather_facts: False
  tasks:
    - name: run insights
      command: redhat-access-insights
      changed_when: false